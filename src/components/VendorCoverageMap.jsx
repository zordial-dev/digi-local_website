import React from 'react';

// Go Global Interactive Map Component (Commented out / disabled as per v4.0.0 API Specification)
export const VendorCoverageMap = () => {
  return (
    <div className="p-4 rounded-xl bg-slate-100 dark:bg-slate-900 border border-slate-200 dark:border-white/10 text-xs font-semibold text-slate-500">
      <p className="font-bold text-slate-700 dark:text-slate-300">⚠️ Go Global Leaflet Map & Radius Circle Selector (Disabled / Commented Out as per v4.0 Specification)</p>
      <p className="text-[11px] mt-1">Location search is now managed via string matching for Area, City, State, and Pincode.</p>
    </div>
  );
};

export default VendorCoverageMap;
